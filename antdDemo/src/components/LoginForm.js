import React,{Component} from 'react';
import {Table,Popconfirm,Button} from 'antd';
import {connect} from 'dva';

class LoginForm extends Component{
    render(){
        return (
            <div><Button>sdsd</Button></div>
        )
    }
}

export default connect()(LoginForm);
